import { displayPopup } from "../utils/popup.js";

export function updateSubtaskCompletion(subtaskId) {
    try {
        // const response = await fetch(``)
    } catch (error) {
        displayPopup(error.message)
    }
}